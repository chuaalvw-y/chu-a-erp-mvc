# ChuA.ERP — MVC UI (Phase 5)

ASP.NET Core MVC presentation layer for ChuA ERP. This is **only** the UI — it owns no
business logic and talks to **ChuA.ERP.Api** strictly over HTTP.

## Purpose

Phase 5 delivers the operator-facing UI for the ERP modules already exposed by the API
(Vendors, Customers, Bills, Invoices, Chart of Accounts, Journal Entries, Purchase Orders,
Sales Orders, Inventory, Workflow, Reports, plus Companies/Users/Roles administration and
a System Health screen).

The UI is responsible for:
- Authentication-aware navigation (sidebar items hidden by role/permission)
- Strongly typed forms with `jquery-validate-unobtrusive` client validation
- Antiforgery on every POST (default `AutoValidateAntiforgeryTokenAttribute`)
- Correlation-id propagation on every outbound API call (`X-Correlation-ID`)
- ProblemDetails error parsing into `Result<T>` for clean UI display
- Toast notifications, breadcrumbs, empty-state messages, error/access-denied pages

It is **not** responsible for: persistence, domain rules, validation that the API also enforces, JWT issuance.

## How it connects to ChuA.ERP.Api

```
┌───────────────┐   HTTPS + Bearer    ┌──────────────────────┐
│ ChuA.ERP.Web  │ ──────────────────► │   ChuA.ERP.Api       │
│      .Mvc     │ ◄────────────────── │  /api/v1/...         │
└───────────────┘   ProblemDetails    └──────────────────────┘
```

- Base address: `Api:BaseUrl` from configuration (default `https://localhost:5001`)
- Every API client derives from `ApiClientBase` and uses `IHttpClientFactory` with a typed
  `HttpClient` registered per client (see `Extensions/ServiceCollectionExtensions.cs`).
- Each request automatically receives:
  - `Authorization: Bearer …` (resolved by `ITokenAcquisitionService`, currently sourced
    from the cookie session's stored `access_token` or a configured dev token)
  - `X-Correlation-ID` (sourced from the inbound request or generated by
    `CorrelationIdAccessor`)
  - `Accept: application/json, application/problem+json`
- Responses are deserialized into the DTOs under `Contracts/Dtos/` (mirrors of
  `ChuA.ERP.Application.DTOs.*`) and wrapped in `Result<T>` / `Result`.
- Non-2xx responses are parsed as `ApiErrorResponse` (the API's `ProblemDetails` subtype)
  and surfaced through `Result.FromProblem(...)`.

The MVC project does **not** take a project reference on `ChuA.ERP.Api`. The contract
surface is mirrored locally so the UI can evolve independently of the API repo.

## Authentication setup

Cookie authentication is the active scheme. OIDC (OpenID Connect) is wired but disabled
until you populate `Oidc:Authority` / `ClientId` / `ClientSecret` in configuration.

### Development (default)

`appsettings.Development.json` ships with:

```json
"Api":  { "UseAuthBypass": true },
"Oidc": { "Enabled": false }
```

This points the MVC at the API's `AuthBypass` scheme — no bearer token is attached and the
API treats every request as a synthetic admin. Use the dev `/Account/Login` page (any user
name works) to issue a local cookie carrying `SystemAdmin` + `CompanyAdmin` roles so the
UI exercises every menu item.

### Enabling OIDC

1. Configure your IdP (Entra ID, Auth0, Keycloak, etc.) with:
   - Redirect URI: `https://your-host/signin-oidc`
   - Post-logout URI: `https://your-host/signout-callback-oidc`
   - PKCE: required
2. Set:
   ```json
   "Oidc": {
     "Enabled": true,
     "Authority": "https://your-idp/",
     "ClientId": "chua-erp-mvc",
     "ClientSecret": "…",  // use user-secrets or env var
     "Scopes": [ "openid", "profile", "email", "chua-erp-api" ],
     "ApiAudience": "chua-erp-api",
     "RequireHttpsMetadata": true
   },
   "Api": { "UseAuthBypass": false }
   ```
3. Restart. The `Sign in` link now redirects to your IdP. OIDC tokens are saved in the
   authentication ticket (`SaveTokens = true`); outside Development the cookie ticket is
   stored server-side so tokens are not serialized into the browser cookie.

### Roles & permissions

- **Roles** drive coarse menu visibility (`<li asp-authorize-role="SystemAdmin">…`).
- **Permissions** drive fine-grained gating (`<li asp-authorize-policy="VendorRead">…`).

Both are sourced from claims on the principal (`role` and `permission`). The MVC also
hydrates a `CurrentUserDto` from `GET /api/v1/users/me` and merges the API-reported
permissions with claim-based permissions.

## API base URL configuration

| Setting             | Default                  | Purpose                                                                          |
|---------------------|--------------------------|----------------------------------------------------------------------------------|
| `Api:BaseUrl`       | `https://localhost:5001` | Root of the API host. `/api/` is appended automatically.                         |
| `Api:Version`       | `v1`                     | Path segment used by API clients (`v1/vendors`, etc.).                           |
| `Api:TimeoutSeconds`| `30`                     | `HttpClient.Timeout`.                                                            |
| `Api:RetryCount`    | `3`                      | Safe-method retry count for transient HTTP failures.                             |
| `Api:UseAuthBypass` | `false`                  | When true, no bearer token is sent. Allowed only in Development.                 |
| `Api:DevelopmentBearerToken` | `null`          | Optional static token for quick local testing without OIDC.                      |

Override per-environment via `appsettings.{Environment}.json`, environment variables
(`Api__BaseUrl=https://api.example.com`), or `dotnet user-secrets`.

## Folder structure

```
src/ChuA.ERP.Web.Mvc/
├── Controllers/             # Thin controllers, one per module + Account/Home/Dashboard/Health
├── Views/
│   ├── Shared/              # _Layout, _LoginPartial, _Navigation, _Breadcrumb, _Toasts, _Pager, _SearchBox, _ValidationSummary, _ValidationScriptsPartial, Error, AccessDenied, NotFound
│   ├── Dashboard/  Vendors/  Customers/  ChartOfAccounts/  JournalEntries/
│   ├── Bills/      Invoices/ PurchaseOrders/ Inventory/   SalesOrders/
│   ├── Workflow/   Reports/  Companies/ Users/ Roles/     Health/  Account/  Home/
├── ViewModels/
│   ├── Vendors/  Customers/  Bills/  ...   # Per-module form + list view models
├── ApiClients/              # Strongly typed HTTP clients (ApiClientBase + 15 typed clients)
├── Contracts/
│   ├── Common/              # Result<T>, Error, ApiErrorResponse, PagedResult<T>, ListQuery
│   └── Dtos/                # Local mirrors of ChuA.ERP.Application.DTOs.*
├── Services/                # ICurrentUserService, ICorrelationIdAccessor, ITokenAcquisitionService
├── Filters/                 # CorrelationIdActionFilter, GlobalExceptionFilter
├── Security/                # AuthorizationPolicies, ApiHeaders (mirrors of API constants)
├── TagHelpers/              # asp-authorize-policy, asp-authorize-role, asp-authenticated
├── Extensions/              # ServiceCollectionExtensions, ControllerExtensions, HtmlHelpers
├── Configuration/           # ApiOptions, OidcOptions, AppEnvironmentOptions
├── Utilities/               # Breadcrumb, ToastMessage
├── Models/                  # ErrorViewModel
├── Program.cs / appsettings*.json / Properties/launchSettings.json
└── wwwroot/{css,js,lib,images}

tests/ChuA.ERP.Web.Mvc.Tests/
├── Contracts/               # Result<T>, ApiErrorResponse, PagedResult tests
├── ApiClients/              # HTTP-level tests using TestHttpMessageHandler
├── Services/                # CorrelationIdAccessor, CurrentUserService
├── TagHelpers/              # AuthorizePolicyTagHelper
├── Filters/                 # GlobalExceptionFilter, CorrelationIdActionFilter
├── ViewModels/              # Form-model FromDto/ToRequest mappings + DataAnnotations
├── Controllers/             # VendorsController as the reference controller test
├── Extensions/              # TempData/AddToast + AddResultErrors
└── Infrastructure/          # TestHttpMessageHandler, TestServices helpers
```

## How to run locally

Prerequisite: **.NET 10 SDK** installed. The API host (`ChuA.ERP.Api`) should be running
on the URL configured in `Api:BaseUrl` (default `https://localhost:5001`), or you can point
the MVC at any running instance.

```bash
# from the repo root
dotnet restore
dotnet build

# run the MVC host
dotnet run --project src/ChuA.ERP.Web.Mvc

# default launch profile binds:
#   https://localhost:7100
#   http://localhost:5100
```

Browse to `https://localhost:7100`. You'll be redirected to `/Account/Login`; submit any
user name to receive a dev cookie and reach the dashboard.

### Pointing at a different API

```bash
# environment variable form
ASPNETCORE_ENVIRONMENT=Development \
Api__BaseUrl=https://api.dev.chua.example.com \
dotnet run --project src/ChuA.ERP.Web.Mvc
```

### Front-end assets

The layout references Bootstrap 5, jQuery, and jquery-validation-unobtrusive from
`wwwroot/lib/...`. Install the LibMan CLI if needed, then restore from the MVC project root:

```bash
cd src/ChuA.ERP.Web.Mvc
libman restore
```

The build does not fail without these libraries, but the UI will be unstyled and client
validation will fall back to server-side only. Bootstrap Icons are loaded from a local
restore when present, otherwise from the jsDelivr CDN.

## How to run tests

```bash
dotnet test tests/ChuA.ERP.Web.Mvc.Tests/ChuA.ERP.Web.Mvc.Tests.csproj
```

Or run the whole solution: `dotnet test ChuA.ERP.MVC.sln`.

The test suite uses **xUnit + FluentAssertions + Moq**. HTTP-level API client tests use a
small `TestHttpMessageHandler` so no live API is required.

Coverage at a glance:
- `Contracts` — Result/PagedResult/ApiErrorResponse mapping
- `ApiClients` — request shape, headers, query strings, ProblemDetails parsing
- `Services` — correlation id, current-user service
- `Filters` — exception filter + correlation id filter
- `TagHelpers` — authorize policy gating
- `ViewModels` — DataAnnotations validation + DTO round-trip mapping
- `Controllers` — controller action smoke tests (success/failure branches, redirects)
- `Extensions` — toast queue + AddResultErrors

## What this layer **does not** do

- No direct database access (no EF Core, no connection strings)
- No domain rules or financial calculations
- No API controllers (the API repo owns those)
- No re-implementation of Phases 1–4 services
