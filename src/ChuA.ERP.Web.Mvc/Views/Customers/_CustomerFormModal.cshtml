@*
    Copyright (c) 2026 Alvin Wilsen Chan Chua
    GitHub: chuaalvw-y
    Licensed under the Alvin Wilsen Chan Chua Proprietary Use-Only License.
    See LICENSE.txt in the project root for full license information.

    Bootstrap modal hosting the existing _Form partial. Returned by /Customers/CreateModal
    and /Customers/EditModal; chua-customers.js injects the response into a host div and
    shows it. On validation failure the controller returns this same partial again with
    the populated ModelState, and the JS swaps the modal contents in place.
*@

@model ChuA.ERP.Web.Mvc.ViewModels.Customers.CustomerFormViewModel

@{
    var isEdit = Model.IsEdit;
    var title = isEdit ? "Edit customer" : "New customer";
    var submitUrl = isEdit
        ? Url.Action("EditModal", "Customers", new { id = Model.Id })
        : Url.Action("CreateModal", "Customers");
}

<div class="modal-dialog modal-lg" role="document">
    <div class="modal-content">
        <form id="customer-modal-form"
              method="post"
              action="@submitUrl"
              data-customer-id="@(Model.Id?.ToString() ?? string.Empty)"
              data-is-edit="@(isEdit.ToString().ToLowerInvariant())">
            @Html.AntiForgeryToken()
            <div class="modal-header">
                <h5 class="modal-title">@title</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body">
                <partial name="_Form" model="Model" />
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-outline-secondary" data-bs-dismiss="modal">Cancel</button>
                <button type="submit" class="btn btn-primary">
                    @(isEdit ? "Save changes" : "Create customer")
                </button>
            </div>
        </form>
    </div>
</div>
