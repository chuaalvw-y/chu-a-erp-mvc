@*
    Copyright (c) 2026 Alvin Wilsen Chan Chua
    GitHub: chuaalvw-y
    Licensed under the Alvin Wilsen Chan Chua Proprietary Use-Only License.
    See LICENSE.txt in the project root for full license information.

    Table-body + pager partial. Rendered as part of Index and as the response of
    /Customers/IndexPartial so chua-customers.js can refresh just the table when search,
    page or sort changes - preserving the shell, breadcrumbs, and search box state.
*@

@model ChuA.ERP.Web.Mvc.ViewModels.Customers.CustomerListViewModel

@if (Model.Page.Items.Count == 0)
{
    @Html.EmptyState("No customers match your search yet.")
}
else
{
    <div class="table-responsive">
        <table class="table table-striped table-hover" id="customers-table">
            <thead><tr>
                <th>Code</th><th>Legal name</th><th>Currency</th><th>Credit limit</th><th>Terms</th><th>Status</th><th class="actions">Actions</th>
            </tr></thead>
            <tbody>
            @foreach (var c in Model.Page.Items)
            {
                <partial name="_CustomerRow" model="c" />
            }
            </tbody>
        </table>
    </div>
    <partial name="_Pager" model="Model.Page" />
}
