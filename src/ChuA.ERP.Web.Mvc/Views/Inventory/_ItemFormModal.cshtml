@*
    Copyright (c) 2026 Alvin Wilsen Chan Chua
    GitHub: chuaalvw-y
    Licensed under the Alvin Wilsen Chan Chua Proprietary Use-Only License.
    See LICENSE.txt in the project root for full license information.
*@

@model ChuA.ERP.Web.Mvc.ViewModels.Inventory.ItemFormViewModel

@{
    var isEdit = Model.IsEdit;
    var title = isEdit ? "Edit item" : "New item";
    var submitUrl = isEdit
        ? Url.Action("EditModal", "Inventory", new { id = Model.Id })
        : Url.Action("CreateModal", "Inventory");
}

<div class="modal-dialog modal-lg" role="document">
    <div class="modal-content">
        <form id="item-modal-form"
              method="post"
              action="@submitUrl"
              data-item-id="@(Model.Id?.ToString() ?? string.Empty)"
              data-is-edit="@(isEdit.ToString().ToLowerInvariant())">
            @Html.AntiForgeryToken()
            <div class="modal-header">
                <h5 class="modal-title">@title</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body">
                <partial name="_Form" model="Model" />
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-outline-secondary" data-bs-dismiss="modal">Cancel</button>
                <button type="submit" class="btn btn-primary">
                    @(isEdit ? "Save changes" : "Create item")
                </button>
            </div>
        </form>
    </div>
</div>
