@model ChuA.ERP.Web.Mvc.ViewModels.Workflow.SubmitApprovalFormViewModel
@{
    ViewData["Title"] = "Submit approval decision";
    var task = ViewData["Task"] as WorkflowApprovalDto;
}
<h1 class="h3 mb-4">Submit approval decision</h1>

@if (task is not null)
{
    <dl class="row">
        <dt class="col-sm-3">Approval id</dt><dd class="col-sm-9"><code>@task.Id</code></dd>
        <dt class="col-sm-3">Step</dt><dd class="col-sm-9">@task.StepNumber</dd>
        <dt class="col-sm-3">Assigned to</dt><dd class="col-sm-9"><code>@task.AssignedUserId</code></dd>
        <dt class="col-sm-3">Due at</dt><dd class="col-sm-9">@(task.DueAt?.ToString("yyyy-MM-dd HH:mm") ?? "—")</dd>
        <dt class="col-sm-3">Current status</dt><dd class="col-sm-9">@Html.StatusBadge(task.Decision)</dd>
    </dl>
}

<form asp-action="Submit" asp-route-id="@Model.Id" method="post" novalidate>
    @Html.AntiForgeryToken()
    <partial name="_ValidationSummary" />
    <input type="hidden" asp-for="Id" />
    <input type="hidden" asp-for="InstanceId" />

    <fieldset class="mb-3">
        <legend class="form-label">Decision</legend>
        <div class="form-check">
            <input class="form-check-input" type="radio" asp-for="Decision" id="decision-approve" value="Approved" />
            <label class="form-check-label" for="decision-approve">Approve</label>
        </div>
        <div class="form-check">
            <input class="form-check-input" type="radio" asp-for="Decision" id="decision-reject" value="Rejected" />
            <label class="form-check-label" for="decision-reject">Reject</label>
        </div>
        <span asp-validation-for="Decision" class="text-danger"></span>
    </fieldset>

    <div class="mb-3">
        <label asp-for="Comment" class="form-label"></label>
        <textarea asp-for="Comment" class="form-control" rows="4" maxlength="2000"></textarea>
        <span asp-validation-for="Comment" class="text-danger"></span>
    </div>

    <div>
        <button type="submit" class="btn btn-primary">Submit decision</button>
        <a asp-action="Details" asp-route-id="@Model.Id" class="btn btn-link">Cancel</a>
    </div>
</form>

@section Scripts { <partial name="_ValidationScriptsPartial" /> }
